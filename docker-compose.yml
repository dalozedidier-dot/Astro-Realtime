services:
  astro-realtime:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: dev
      ASTRO_ENGINE: builtin
      DEFAULT_ZODIAC: tropical
      DEFAULT_HOUSE_SYSTEM: whole_sign
      CACHE_TTL_SECONDS: 15
      ALLOW_DOCS: "true"
